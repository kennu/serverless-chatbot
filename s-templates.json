{
  "environment": {
    "SERVERLESS_STAGE": "${stage}",
    "SERVERLESS_DATA_MODEL_STAGE": "${stage}",
    "SERVERLESS_PROJECT_NAME": "${project}",
    "FACEBOOK_BOT_VERIFY_TOKEN": "${FACEBOOK_BOT_VERIFY_TOKEN}",
    "FACEBOOK_BOT_PAGE_ACCESS_TOKEN": "${FACEBOOK_BOT_PAGE_ACCESS_TOKEN}"
  }
}
